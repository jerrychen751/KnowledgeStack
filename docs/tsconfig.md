Nest API uses NodeNext for both module and moduleResolution, while Next.js web application uses ESNext and Bundler.

With NodeNext, Node.js executes the emitted files directly. With ESNext, Next.js bundles the application.